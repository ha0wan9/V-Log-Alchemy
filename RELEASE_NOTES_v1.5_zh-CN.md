# V-Log Alchemy v1.5

## Panasonic Standard 高色度校正

issue #12 指出 Panasonic `Standard -> V-Log` 适配器会让高饱和青色和蓝绿色
产生不合理的低红通道；该异常只启用转换 LUT、尚未应用任何风格 LUT 时就已出现。

v1.5 对全部 10 个 Panasonic Standard 适配器应用公共的实测 V-Log/V-Gamut
输出校正，覆盖 GH6、S5II/S5IIX、G9II、GH7、S9、S1IIE、S1RII、S1II 和
DC-L10。各机型仍使用自己的 Standard 伪逆；新校正只位于该机型专用步骤之后，
工作在共同的 Panasonic V-Log/V-Gamut 输出域中。

校正使用带 ridge 正则的对手色度多项式，强度为 80%。所有严格中性 RGB 保持
不变，最终 `[0,1]` 裁切前的 RGB 平均码值保持不变；没有使用硬性的逐通道
`0.125` 下限。

## S1RII 受控验证

拟合数据来自 DC-S1RM2/S1RII 固件 1.5：SpyderCHECKR 24、固定室内光源、固定
自定义白平衡、ISO 800、f/5.6 和三档曝光。另保留独立场景及旧九组配对用于验证。

| 验证集 | 校正前 | 校正后 |
|---|---:|---:|
| 按曝光留一的彩色色块平均 RGB 距离 | 0.1152 | 0.0942 |
| 按曝光留一的青色色块 | 0.1431 | 0.0652 |
| 独立场景稳定区域 | 0.0607 | 0.0565 |
| 独立场景青色区域 | 0.1624 | 0.0616 |
| 旧九组配对整体平均 RGB 距离 | 0.01748 | 0.01593 |
| 旧九组配对青色区域 | 0.06894 | 0.03999 |

以上是归一化 `[0,1]` V-Log 输出码值中的 RGB 欧氏距离，不是感知 Delta E。
旧九组配对校正后的平均每通道绝对误差为 `0.00805`，约等于 10-bit 的 `8.2`
个码值。

S1RII 已完成受控定量验证；S9 在 issue #12 中有同类修正前症状的独立实拍反馈。
其它机型虽然使用相同的固定 V-Log/V-Gamut 终点，仍需补充对应机型的
Standard/原生 V-Log 配对验证。

## 可复现性与完整性

- 新增 `Calibration/PanasonicVLogOutput.json`，记录系数、约束、源文件哈希、
  校正后哈希和验证指标。
- 新增 `Tools/apply_panasonic_vlog_output_correction.py`；输入均有哈希锁定，
  会拒绝或安全识别重复校正。
- 更新全部 10 个逐机型报告、manifest 和 `SHA256SUMS.txt`。
- 强制发布的 Panasonic LUT 使用 LF 换行，确保原始 SHA-256 跨平台稳定。
- 使用新版基础转换重新生成 S1RII 合并示例 LUT 和全分辨率对比；两个示例的
  双 LUT 与单个合并 LUT 平均差异都低于一个 8-bit 码值。
- 13 项回归测试通过，全部 10 个 LUT 都能从声明的未校正 v1.3 源哈希逐字节复现。

## 限制

Standard 已裁切的信息、色域压缩和非唯一反解区域仍然不可恢复。在相同名义
ISO、光圈和快门下，本次 S1RII 测试的原生 V-Log RAW 信号约为 Standard 的
`0.397x`，相差约 `1.33` 档采集/增益路径。输出 LUT 无法复制原生 V-Log 的
噪声、高光余量或曝光索引行为。
